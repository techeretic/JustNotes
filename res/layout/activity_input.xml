<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.pshetye.justnotes.InputActivity"
    android:background="?attr/backgroundcolor">

    <include
        android:id="@+id/my_awesome_toolbar"
        layout="@layout/activity_toolbar"/>

	<EditText
		android:id="@+id/editText0"
		android:layout_width="match_parent"
		android:layout_height="wrap_content"
		android:layout_marginStart="10dp"
		android:layout_marginEnd="15dp"
		android:layout_marginLeft="10dp"
		android:layout_marginRight="15dp"
		android:layout_marginTop="15dp"
		android:paddingTop="15dp"
		android:paddingStart="15dp"
		android:paddingEnd="15dp"
		android:gravity="top|start"
		android:isScrollContainer="true"
		android:hint="@string/title_hint"
		android:theme="@style/Base.Widget.AppCompat.EditText"
		android:textAppearance="@style/TextAppearance.AppCompat.Title"
		android:textColor="?attr/eTextColor"
		android:textColorHint="?attr/hTextColor"
		android:inputType="textShortMessage|textMultiLine|textCapSentences"
        android:layout_below="@id/my_awesome_toolbar" />
    <EditText
        android:id="@+id/editText1"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginStart="10dp"
        android:layout_marginEnd="15dp"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="15dp"
        android:layout_marginBottom="15dp"
        android:paddingTop="15dp"
        android:paddingBottom="15dp"
        android:paddingStart="15dp"
        android:paddingEnd="15dp"
        android:gravity="top|start"
    	android:isScrollContainer="true"
        android:hint="@string/note_hint"
        android:theme="@style/Base.Widget.AppCompat.EditText"
        android:textColor="?attr/eTextColor"
        android:textColorHint="?attr/hTextColor"
        android:inputType="textLongMessage|textMultiLine|textWebEditText|textCapSentences"
        android:layout_below="@id/editText0" />

</RelativeLayout>